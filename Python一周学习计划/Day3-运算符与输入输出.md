# Day 3：运算符与输入输出

**今日目标（约 2 小时）**

1. 掌握算术、比较、逻辑三类运算符
2. 学会用 `input()` 接收用户输入
3. 完成第一个交互程序：两数计算器

---

## 1. 算术运算符（约 25 分钟）

| 运算符 | 含义 | 例子 | 结果 |
|--------|------|------|------|
| `+` | 加 | `3 + 2` | `5` |
| `-` | 减 | `3 - 2` | `1` |
| `*` | 乘 | `3 * 2` | `6` |
| `/` | 除 | `7 / 2` | `3.5`（结果总是小数） |
| `//` | 整除（舍掉小数） | `7 // 2` | `3` |
| `%` | 取余数 | `7 % 2` | `1` |
| `**` | 乘方 | `2 ** 3` | `8`（2 的 3 次方） |

动手试试：

```python
print(10 + 3)    # 13
print(10 / 3)    # 3.3333333333333335
print(10 // 3)   # 3
print(10 % 3)    # 1（10 除以 3 余 1）
print(2 ** 10)   # 1024
```

`%` 取余数特别常用，比如判断一个数是奇数还是偶数：`n % 2` 结果是 `0` 就是偶数，是 `1` 就是奇数。

## 2. 比较运算符（约 15 分钟）

比较两个值，结果是布尔值 `True` 或 `False`：

| 运算符 | 含义 |
|--------|------|
| `==` | 等于（注意是两个等号！） |
| `!=` | 不等于 |
| `>` | 大于 |
| `<` | 小于 |
| `>=` | 大于等于 |
| `<=` | 小于等于 |

```python
age = 18
print(age == 18)   # True
print(age > 20)    # False
print(age != 18)   # False
```

**新手必踩的坑**：`=` 是赋值（放进盒子），`==` 是比较（判断是否相等）。写判断时漏掉一个等号是最常见的错误，明天学 if 时千万注意。

## 3. 逻辑运算符（约 15 分钟）

把多个条件组合起来：

- `and`：两边都成立才为真
- `or`：两边有一个成立就为真
- `not`：取反

```python
age = 18
score = 85

print(age >= 18 and score >= 60)   # True（成年了 并且 及格了）
print(age < 18 or score >= 90)     # False（未成年，也没到90分）
print(not age >= 18)               # False（取反）
```

## 4. 赋值与复合赋值（约 10 分钟）

```python
count = 0
count = count + 1   # 把 count 的值加 1 再放回去
print(count)        # 1
```

`count = count + 1` 这种写法太常见了，Python 提供了简写：

```python
count = 0
count += 1    # 等同于 count = count + 1
count += 5
print(count)  # 6
```

同理还有 `-=`、`*=`、`/=`。

## 5. input() 接收用户输入（约 25 分钟）

前面的程序都是"自言自语"，现在让程序和用的人互动。`input()` 会在屏幕上显示提示文字，然后**停下来等用户打字**，用户按回车后，把用户输入的内容作为结果返回。

```python
name = input("你叫什么名字？")
print(f"你好，{name}！")
```

运行后屏幕上出现 `你叫什么名字？`，光标闪烁等你输入。输入 `小红` 回车，显示：`你好，小红！`

**非常重要的一点：`input()` 拿到的永远是字符串。** 哪怕用户输入的是数字：

```python
age = input("你几岁？")
print(type(age))   # <class 'str'>，是字符串！
```

要做数学运算，必须先用 `int()` 或 `float()` 转换：

```python
age = int(input("你几岁？"))
print(f"明年你{age + 1}岁")
```

也可以分开写，看得更清楚：

```python
text = input("你几岁？")
age = int(text)
print(f"明年你{age + 1}岁")
```

## 动手练习：两数计算器（约 20 分钟）

新建文件 `day3计算器.py`，实现：让用户依次输入两个数字，程序输出这两个数的加、减、乘、除结果。

运行示例（用户输入 10 和 4）：

```
请输入第一个数字：10
请输入第二个数字：4
10.0 + 4.0 = 14.0
10.0 - 4.0 = 6.0
10.0 * 4.0 = 40.0
10.0 / 4.0 = 2.5
```

> 提示：用 `float()` 而不是 `int()` 转换输入，这样用户输入小数时也能正常工作。
> 先自己写。做完之后可以对照参考答案：`answers/day3计算器.py`

## 今日小结

- 算术：`+ - * / // % **`；`%` 可以判断奇偶
- 比较：`==`（两个等号）`!= > < >= <=`，结果是 True/False
- 逻辑：`and`、`or`、`not`
- 复合赋值：`count += 1`
- `input()` 等待用户输入，**返回的永远是字符串**，算数前先 `int()` 或 `float()` 转换

## 如果卡住了怎么办

| 症状 | 可能原因和解决办法 |
|------|------|
| `ValueError: invalid literal for int()` | 用户在需要数字的地方输入了文字。练习阶段先确保自己输入的是数字；明天学完 if 后可以处理这种情况 |
| `TypeError: can only concatenate str...` 或数字相加结果不对（如 1+2 变成 12） | 忘记把 `input()` 的结果转成数字，`"1" + "2"` 是字符串拼接。加上 `int()` 或 `float()` |
| 程序卡在原地没反应 | 不是卡死，是 `input()` 在等你输入。在下方面板里点一下，打字后按回车 |
| 比较时报 `SyntaxError` | 检查是不是把 `==` 写成了 `=` |
