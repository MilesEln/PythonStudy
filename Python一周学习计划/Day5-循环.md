# Day 5：循环

**今日目标（约 2 小时）**

1. 掌握 for 循环和 `range()`
2. 掌握 while 循环、`break` 和 `continue`
3. 完成第一个有成就感的小项目：猜数字游戏

---

## 1. for 循环：把一件事重复做（约 25 分钟）

如果想打印 5 次"加油"，笨办法是写 5 行 `print`。循环让代码自己重复：

```python
for i in range(5):
    print("加油！")
```

运行结果：打印 5 行"加油！"

`range(5)` 会依次产生 0、1、2、3、4 这 5 个数，循环每跑一轮，变量 `i` 就取其中一个值。看看 `i` 的变化：

```python
for i in range(5):
    print(i)
```

运行结果：

```
0
1
2
3
4
```

注意：**从 0 开始，到 5 之前结束**（不包含 5）。

`range()` 的三种用法：

```python
range(5)        # 0, 1, 2, 3, 4
range(1, 6)     # 1, 2, 3, 4, 5（从1开始，到6之前结束）
range(2, 11, 2) # 2, 4, 6, 8, 10（第三个数是步长，每次加2）
```

和 if 一样，`for ... :` 下面缩进的代码才是循环体，每轮都会执行。

**小例子：计算 1 加到 100**

```python
total = 0
for i in range(1, 101):
    total += i
print(total)   # 5050
```

## 2. while 循环：条件成立就一直做（约 20 分钟）

for 适合"知道要循环几次"的场景；while 适合"不知道几次，但知道什么时候停"的场景：

```python
count = 1
while count <= 5:
    print(f"第{count}次")
    count += 1
```

运行结果：打印"第1次"到"第5次"。

执行逻辑：每轮先检查 `count <= 5`，成立就执行循环体，然后再检查……直到条件不成立为止。

**大坑提醒**：如果循环体里忘了 `count += 1`，条件永远成立，程序会永远循环下去（死循环）。万一写出来了，在终端按 `Control + C` 强制停止程序。

## 3. break 和 continue（约 15 分钟）

- `break`：**立刻结束整个循环**，不管循环还没跑完
- `continue`：**跳过本轮**，直接进入下一轮

```python
for i in range(1, 11):
    if i == 3:
        continue    # 跳过 3
    if i == 8:
        break       # 到 8 就结束整个循环
    print(i)
```

运行结果：`1 2 4 5 6 7`（3 被跳过，8 及以后没机会执行）

## 4. 小练习：乘法表（约 20 分钟）

新建 `day5乘法表.py`，让用户输入一个数字，打印它的乘法表（乘 1 到 9）。

输入 `7` 的运行结果应该是：

```
7 × 1 = 7
7 × 2 = 14
...
7 × 9 = 63
```

> 先自己写。做完之后可以对照参考答案：`answers/day5乘法表.py`

## 5. 今日项目：猜数字游戏（约 30 分钟）

规则：程序心里想一个 1~100 的数字，用户来猜。每次猜完程序提示"大了"或"小了"，猜对为止。

这会把本周学的知识都串起来：`input()`、`int()` 转换、if 判断、while 循环、`break`。

提示：`while True:` 表示"无条件一直循环"，靠循环体里的 `break` 跳出——这是"不知道要猜几次"场景的经典写法。

**挑战升级（选做）**：让程序每次随机想一个数字。在文件最上面加一行 `import random`，然后用 `random.randint(1, 100)` 生成 1~100 的随机数作为答案，每次运行答案都不一样。

> 先自己挑战。做完或实在卡住时，再看参考答案：`answers/day5猜数字.py`

## 今日小结

- `for i in range(n):` 重复 n 次；`range` 从 0 开始、不包含结尾
- `while 条件:` 条件成立就一直循环，记得在循环体里改变条件相关的变量，否则死循环
- `break` 结束循环，`continue` 跳过本轮
- `Control + C` 可以强制停止程序

## 如果卡住了怎么办

| 症状 | 可能原因和解决办法 |
|------|------|
| 程序刷屏停不下来 | 死循环。按 `Control + C` 停止，检查循环体里有没有更新条件变量（如 `count += 1`） |
| 循环一次都没执行 | 检查 while 的条件是不是一开始就不成立；检查 `range()` 的参数，比如 `range(5, 1)` 是空的 |
| 结果差 1（比如少循环一次） | `range` 不包含结尾数。想循环 1~100 就写 `range(1, 101)` |
| `IndentationError` | 循环体忘缩进，或循环体里的 if 需要再缩进一级 |
